/**
 * Initial inventory data used to populate the system when no products are found.
 * Currently initialized as an empty array, but can be pre-filled with seed data.
 */
export const initialInventory = [];